# Copy to functions/.env and fill in. functions/.env is gitignored.

# Where contact-form emails are delivered.
CONTACT_TO=connect@arjusingh.com

# NOTE: SMTP credentials are NOT set here anymore — they're configured once when
# you install the "Trigger Email" extension (firebase ext:install
# firebase/firestore-send-email). The extension holds the SMTP connection.
